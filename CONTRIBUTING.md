# 贡献

日常开发使用短分支和 PR，合入 `main` 后删除短分支。较大的产品行为变化先用 Issue 说明场景，小修复可以直接提 PR。

1. 按 README 准备环境；运行 `./tools/check_fast.sh`。
2. 修改功能并增加有意义的离线回归检查。不要在测试样本里放真实用户信息。
3. 运行 `./tools/build.sh --compile`，更新受影响的说明。
4. PR 描述触发条件、行为变化、检查结果，以及需要人工确认的项目。

CI 必须通过。涉及真实协议、共享、音频或权限的变更，由维护者执行相应本地集成检查；贡献者不需要维护者账号、证书或私钥。不要为了让 PR 通过而放宽“自有空频道”和音频隔离规则。

版本只在发版准备时通过 `tools/bump.sh` 升级，普通 PR 不修改版本号。把用户可见变化写入 CHANGELOG 的 Unreleased 区域。

## 本地隐私检查

```sh
git config --local core.hooksPath .githooks
python3 tools/audit_public.py --history
```

提交钩子审查暂存区，推送钩子审查工作区和所有本地引用可达的历史。钩子可以被绕过，因此 CI 仍会执行同一检查。检查器属于防误传措施，不是绝对无泄露证明。

不要使用私人邮箱作为提交邮箱；可使用 GitHub 的 noreply 地址。不要提交屏幕截图中的真实聊天、头像、邀请链接、账号信息或认证材料。提交内容应是你有权以本项目许可贡献的作品。

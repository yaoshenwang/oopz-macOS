# 隐私与安全

## 数据边界

- 用户会话与运行时协议材料仅保存在本机应用数据目录。会话文件权限为 0600，新建目录为 0700。
- 正常使用会连接 OOPZ 服务和 RTC 服务商；这不是离线软件。项目没有自建遥测接收服务。
- 应用阶段日志通过 `DiagnosticMessage` 记录，字符串插值全部替换为 `[private]`，不序列化动态用户名、token、消息内容或 URL。
- `tools/collect_diagnostics.py` 只导出新格式日志中的版本和消息字段，忽略历史自由文本日志，不复制会话、SDK 原始日志或系统日志。上传前仍应自己检查导出文件。
- 编译时重映射源码路径，发布前审查 Git 历史、源文件和未签名 App。Developer ID 证书主体本身会暴露签名者身份；签名文件不能被描述为匿名产物。

## 报告问题

不要在公开 Issue 里粘贴凭据、原始抓包、完整请求头、用户数据或私钥。如仓库已启用 GitHub 私密漏洞报告，请从 Security 页面使用 **Report a vulnerability**。若该入口尚不可用，只提交不含敏感细节的联系请求，等待维护者建立私下渠道。

扫描脚本可能漏检未知格式。维护者可以在仓库外建立私有字符串清单，通过 `OOPZ_AUDIT_DENYLIST` 在本地检查；清单本身不得上传。暴露了有效的个人凭据时，先撤销／轮换，再处理历史与产物。
